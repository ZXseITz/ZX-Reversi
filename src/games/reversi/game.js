const games = [];

module.exports = {
    create: (white, black) => {
        //todo create game
        console.log(`created new game: ${white} vs ${black}`);
        return 0;
    },
    set: (game, player, coord) => {
        return 0;
    }
}